export type Banner = { public_id: string; name: string; placement: string; headline: string | null; subheading: string | null; cta_label: string | null; cta_url: string | null; desktop_image_url: string; mobile_image_url: string | null; alt_text: string | null; sort_order: number; is_active: boolean; starts_at: string | null; ends_at: string | null }
export type HomepageSection = { public_id: string; name: string; type: 'hero_banner' | 'featured_categories' | 'featured_products' | 'promotional_banner' | 'brand_story'; is_active: boolean; sort_order: number; configuration: Record<string, string | null>; categories: Array<{ public_id: string; name: string; slug: string }>; products: Array<{ public_id: string; name: string; slug: string }> }
export type SectionInput = Omit<HomepageSection, 'public_id' | 'categories' | 'products'> & { category_public_ids: string[]; product_public_ids: string[] }
