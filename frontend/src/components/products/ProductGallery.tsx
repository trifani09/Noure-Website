"use client";
import {useEffect,useState} from "react";import {SafeImage} from "@/components/common/SafeImage";import type {ProductDetail} from "@/types/catalog";
export function ProductGallery({product}:{product:ProductDetail}){const images=[...product.images].sort((a,b)=>a.sort_order-b.sort_order);const[active,setActive]=useState(0);const[zoomed,setZoomed]=useState(false);useEffect(()=>{if(!zoomed)return;const close=(event:KeyboardEvent)=>{if(event.key==="Escape")setZoomed(false)};document.addEventListener("keydown",close);document.body.style.overflow="hidden";return()=>{document.removeEventListener("keydown",close);document.body.style.overflow=""}},[zoomed]);if(!images.length)return <div className="grid aspect-[4/5] place-items-center bg-ivory text-xs uppercase tracking-widest text-taupe">Noure</div>;const current=images[active]??images[0];return <div><button type="button" onClick={()=>setZoomed(true)} aria-label={`Zoom ${current.alt_text??product.name}`} className="focus-ring group relative block aspect-[4/5] w-full cursor-zoom-in overflow-hidden bg-ivory"><SafeImage fill priority sizes="(max-width:1024px) 100vw, 58vw" className="object-cover transition duration-700 group-hover:scale-[1.02]" src={current.url} alt={current.alt_text??product.name}/><span className="absolute bottom-4 right-4 bg-paper/90 px-3 py-2 text-[10px] uppercase tracking-widest">View larger</span></button>{images.length>1&&<div className="mt-3 flex snap-x gap-3 overflow-x-auto pb-2" aria-label="Product images">{images.map((image,index)=><button type="button" aria-label={`Show image ${index+1} of ${images.length}`} aria-pressed={index===active} key={`${image.url}-${index}`} onClick={()=>setActive(index)} className={`focus-ring relative aspect-[4/5] w-20 shrink-0 snap-start overflow-hidden border-2 ${index===active?"border-ink":"border-transparent opacity-70 hover:opacity-100"}`}><SafeImage fill sizes="80px" className="object-cover" src={image.url} alt=""/></button>)}</div>}{zoomed&&<div role="dialog" aria-modal="true" aria-label="Enlarged product image" className="fixed inset-0 z-[70] grid place-items-center bg-ink/95 p-4 md:p-10" onClick={()=>setZoomed(false)}><button type="button" aria-label="Close image zoom" className="focus-ring absolute right-5 top-5 z-10 px-3 py-2 text-3xl text-paper" onClick={()=>setZoomed(false)}>×</button><div className="relative h-full w-full max-w-5xl" onClick={event=>event.stopPropagation()}><SafeImage fill priority sizes="100vw" className="object-contain" src={current.url} alt={current.alt_text??product.name}/></div></div>}</div>}
