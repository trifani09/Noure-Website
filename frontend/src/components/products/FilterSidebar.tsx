import type {Category} from "@/types/catalog";
export function FilterSidebar({categories,values}:{categories:Category[];values:Record<string,string|undefined>}){return <aside className="border-t border-line pt-5 lg:border-t-0 lg:border-r lg:pr-8 lg:pt-0"><form action="/products" className="space-y-7"><div><label className="eyebrow text-muted" htmlFor="search">Search</label><input id="search" name="search" defaultValue={values.search} placeholder="Dress, blouse…" className="mt-3 w-full border-b border-line bg-transparent py-2 text-sm outline-none focus:border-plum"/></div><div><label className="eyebrow text-muted" htmlFor="category">Category</label><select id="category" name="category" defaultValue={values.category} className="mt-3 w-full border border-line bg-paper p-3 text-sm"><option value="">All categories</option>{categories.map(category=><option key={category.public_id} value={category.slug}>{category.name}</option>)}</select></div><div><p className="eyebrow text-muted">Price</p><div className="mt-3 grid grid-cols-2 gap-2"><input aria-label="Minimum price" name="min_price" defaultValue={values.min_price} type="number" min="0" placeholder="Min" className="w-full border border-line bg-paper p-3 text-sm"/><input aria-label="Maximum price" name="max_price" defaultValue={values.max_price} type="number" min="0" placeholder="Max" className="w-full border border-line bg-paper p-3 text-sm"/></div></div><div><label className="eyebrow text-muted" htmlFor="availability">Availability</label><select id="availability" name="availability" defaultValue={values.availability} className="mt-3 w-full border border-line bg-paper p-3 text-sm"><option value="">All pieces</option><option value="available">Available</option><option value="unavailable">Unavailable</option></select></div><input type="hidden" name="sort" value={values.sort??"newest"}/><button className="w-full bg-ink px-4 py-3 text-xs font-semibold uppercase tracking-widest text-paper">Apply filters</button><a href="/products" className="block text-center text-xs text-muted underline">Clear filters</a></form></aside>}
