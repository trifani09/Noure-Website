import Link from "next/link";import type {Pagination as PaginationType} from "@/types/catalog";
export function Pagination({pagination,params,path="/products"}:{pagination:PaginationType;params:Record<string,string|undefined>;path?:string}){if(pagination.last_page<=1)return null;const href=(page:number)=>{const query=new URLSearchParams();Object.entries(params).forEach(([key,value])=>{if(value)query.set(key,value)});query.set("page",String(page));return `${path}?${query}`};return <nav className="mt-16 flex items-center justify-between border-t border-line pt-6 text-xs uppercase tracking-widest"><Link className={pagination.current_page<=1?"pointer-events-none opacity-30":""} href={href(pagination.current_page-1)}>← Previous</Link><span className="text-muted">Page {pagination.current_page} of {pagination.last_page}</span><Link className={pagination.current_page>=pagination.last_page?"pointer-events-none opacity-30":""} href={href(pagination.current_page+1)}>Next →</Link></nav>}
