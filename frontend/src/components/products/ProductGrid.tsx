import type {ProductSummary} from "@/types/catalog";import {ProductCard} from "./ProductCard";import {EmptyState} from "../common/EmptyState";
export function ProductGrid({products}:{products:ProductSummary[]}){return <div className="grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-3 lg:grid-cols-4 lg:gap-x-6 lg:gap-y-14">{products.length?products.map((product,index)=><ProductCard key={product.public_id} product={product} priority={index<4}/>):<EmptyState/>}</div>}
