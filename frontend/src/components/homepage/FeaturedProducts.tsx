import Link from "next/link";import type {HomepageSection} from "@/types/catalog";import {SectionHeading} from "../common/SectionHeading";import {ProductGrid} from "../products/ProductGrid";
export function FeaturedProducts({section}:{section:HomepageSection}){if(!section.products.length)return null;return <section className="page-shell py-20 md:py-28"><SectionHeading eyebrow="Selected for you" title={section.configuration.heading??section.name} body={section.configuration.body}/><div className="mt-12"><ProductGrid products={section.products}/></div><div className="mt-12 text-center"><Link href={section.configuration.cta_url??"/products"} className="border-b border-ink pb-1 text-xs font-semibold uppercase tracking-[.18em]">{section.configuration.cta_label??"Discover the collection"}</Link></div></section>}
