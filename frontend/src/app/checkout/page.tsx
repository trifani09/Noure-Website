import type {Metadata} from "next";import {ContentPlaceholder} from "@/features/content";
export const metadata:Metadata={title:"Checkout",description:"Noure checkout route foundation."};
export default function CheckoutPage(){return <ContentPlaceholder eyebrow="Commerce foundation" title="Checkout" description="Checkout and payment functionality are intentionally reserved for a later commerce phase."/>}
