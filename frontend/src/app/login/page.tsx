"use client";
import Link from "next/link";import {useRouter,useSearchParams} from "next/navigation";import {FormEvent,useEffect,useState} from "react";import {AuthShell} from "@/components/auth/AuthShell";import {FormField} from "@/components/auth/FormField";import {useAuth} from "@/components/auth/AuthProvider";import {AuthApiError} from "@/services/auth-api";
export default function LoginPage(){const{customer,loading:sessionLoading,login}=useAuth();const router=useRouter();const searchParams=useSearchParams();const[error,setError]=useState("");const[saving,setSaving]=useState(false);useEffect(()=>{if(!sessionLoading&&customer)router.replace("/account")},[customer,sessionLoading,router]);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");setSaving(true);const data=new FormData(event.currentTarget);try{await login({email:String(data.get("email")),password:String(data.get("password")),remember:data.get("remember")==="on"});const next=searchParams.get("next");router.replace(next?.startsWith("/")&&!next.startsWith("//")?next:"/account")}catch(caught){setError(caught instanceof AuthApiError?caught.message:"Unable to sign in. Please try again.")}finally{setSaving(false)}}return <AuthShell eyebrow="Your account" title="Welcome back" footer={<>New to Noure? <Link className="text-ink underline underline-offset-4" href="/register">Create an account</Link></>}><form onSubmit={submit} className="space-y-5">{error&&<div role="alert" className="border border-rose/60 bg-ivory p-3 text-sm text-plum">{error}</div>}<FormField label="Email" name="email" type="email" autoComplete="email" required/><FormField label="Password" name="password" type="password" autoComplete="current-password" required/><div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2"><input name="remember" type="checkbox"/> Remember me</label><Link href="/forgot-password" className="underline underline-offset-4">Forgot password?</Link></div><button disabled={saving||sessionLoading} className="focus-ring w-full bg-ink px-5 py-4 text-xs font-semibold uppercase tracking-[.18em] text-paper disabled:opacity-50">{saving?"Signing in…":"Sign in"}</button></form></AuthShell>}
