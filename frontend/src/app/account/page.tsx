import type {Metadata} from "next";import {ContentPlaceholder} from "@/features/content";
export const metadata:Metadata={title:"Account",description:"Noure customer-account route foundation."};
export default function AccountPage(){return <ContentPlaceholder eyebrow="Account foundation" title="Your account" description="Customer sign-in and account features are intentionally reserved for a later phase."/>}
