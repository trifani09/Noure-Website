import type {Metadata} from "next";import {ContentPlaceholder} from "@/features/content";
export const metadata:Metadata={title:"Shopping bag",description:"Noure shopping-bag route foundation."};
export default function CartPage(){return <ContentPlaceholder eyebrow="Commerce foundation" title="Your bag" description="Cart functionality is intentionally reserved for a later commerce phase."/>}
